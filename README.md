# shipping-service
# shipping-service  The **shipping-service** manages shipping orders and tracking.  ## Endpoints - `GET /health` — service status - `POST /shipping/create` — create shipping order (sample) - `GET /shipping/:orderId` — get shipping status (sample) - `POST /shipping/update` — update shipping status (sample)  ## Tracing This service reports telemetry 
